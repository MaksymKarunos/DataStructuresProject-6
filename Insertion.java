public class Insertion{
    void InsertionSort(int[] arr){
        int n = arr.length;
        for ( int i = 0; i < n; i++){
        int firstUnsorted = arr[i];
        int j = i - 1; // first element to compare to
        }
    }
}